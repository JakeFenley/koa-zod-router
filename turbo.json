{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": [".nyc_output/**", "coverage/**"]
    },
    "lint": {
      "outputs": [".eslintcache"]
    }
  }
}
